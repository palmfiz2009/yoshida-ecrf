name: Wake up Streamlit
on:
  schedule:
    - cron: '0 * * * *' # 1時間に1回実行
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping my app
        run: curl -I https://yoshida-ecrf.streamlit.app # ここを各アプリのURLに書き換え
